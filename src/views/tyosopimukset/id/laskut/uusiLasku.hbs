<h2 class="font-medium text-xl">Uusi lasku</h2>

<br />

<p>Työsopimuksen numero: {{tyosopimus_id}}</p>

<form hx-post="/laskut" hx-target="#modal-content" hx-vals='{"tyosuoritus_id": "{{tyosopimus_id}}"}'>
  <div class="mb-5">
    <label>
      <div class="mb-2 text-sm font-medium">Laskun summa €:</div>
      <input type="number" name="summa" min="0" step="0.01" class="modal-input modal-i" required>
    </label>
  </div>

  <div class="mb-5">
    <label>
      <div class="mb-2 text-sm font-medium">Eräpäivä</div>
      <input type="date" name="era_pvm" min="{{today}}" value="{{default_era_pvm}}" class="modal-input modal-i"
        required>
    </label>
  </div>

  <div class="flex items-center">
    <button type="submit" class="button-submit">
      Tallenna
    </button>
    <div class="p-1"></div>
    <button hx-get="/tyhja" hx-target="#modal-content" class="button-cancel">
      Peruuta
    </button>
  </div>
</form>