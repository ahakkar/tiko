<div class="container-titlebar">
  <div class="text-2xl my-auto">{{title}}</div>

  <div>
    {{#if writeAccess}}
    <button hx-get="{{hxGetUrl}}" hx-target="{{hxTarget}}" hx-push-url="{{hxPushUrl}}"
      class="titlebar-button {{#if isDisabled}}opacity-50 cursor-not-allowed{{/if}}" {{#if isDisabled}}disabled{{/if}}>
      {{buttonText}}
    </button>
    {{/if}}
  </div>
</div>